# Chat UI Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** A clean local web chat where the user types a schedule, sees the read-back, and Approves (real BLE write) or refines by typing again.

**Architecture:** FastAPI app on the Mac serving one static chat page + two endpoints (`/preview`, `/apply`) that wrap the existing core (`build_schedule`, `readback`, `write_schedule`). Thin front door; no core changes.

**Tech Stack:** FastAPI + uvicorn (runtime), httpx (test client), existing Python core, vanilla HTML/CSS/JS frontend.

## Global Constraints

- Python 3.11+. New deps: `fastapi`, `uvicorn` (runtime); `httpx` (dev, for TestClient).
- Web layer is a thin wrapper — do NOT change `core.py`, `parser.py`, `ble_writer.py`, `encoder.py`, `validator.py`, `readback.py`, `registry.py`, `model.py`.
- Registry path from env `SWITCHBOT_DEVICES` (default `devices.yaml`); `OPENAI_API_KEY` from env.
- Server binds host `0.0.0.0`, port from env `PORT` (default 8000).
- `/apply` writes the EXACT schedule posted from the browser (rebuilt from JSON, re-validated) — never re-parses.
- Errors are returned as `{"ok": false, "error": <message>}`, never an unhandled 500.
- Tests are network-free and hardware-free: inject the parser via the module-level `_completion_fn` seam; monkeypatch `write_schedule`.

---

## File Structure
```
src/switchbot_scheduler/web/
  __init__.py
  app.py                 # FastAPI app: GET / , POST /preview , POST /apply , main()
  static/index.html      # the chat UI (Task 2 replaces the placeholder)
tests/test_web.py        # TestClient tests for /preview and /apply
pyproject.toml           # add deps + [project.scripts] switchbot-ui
```

---

## Task 1: Backend — FastAPI app + endpoints

**Files:**
- Create: `src/switchbot_scheduler/web/__init__.py`, `src/switchbot_scheduler/web/app.py`, `src/switchbot_scheduler/web/static/index.html` (placeholder), `tests/test_web.py`
- Modify: `pyproject.toml` (deps + console script)

**Interfaces:**
- Consumes: `build_schedule(prompt, registry, completion_fn=None)` (core), `readback(schedule)` (readback), `write_schedule(schedule, registry)` (ble_writer), `validate(schedule, registry)`/`ScheduleError` (validator), `Registry.load(path)` (registry), `Schedule/DeviceSchedule/Event` (model).
- Produces: `app` (FastAPI), module-level `_completion_fn` (None by default; test seam), `schedule_to_json`/`schedule_from_json`, `main()` entry point.

- [ ] **Step 1: Add deps + console script to `pyproject.toml`**

In `[project] dependencies` add `"fastapi>=0.110"`, `"uvicorn>=0.29"`. In the dev extra add `"httpx>=0.27"`. Add:
```toml
[project.scripts]
switchbot-schedule = "switchbot_scheduler.cli:main"
switchbot-ui = "switchbot_scheduler.web.app:main"
```
(Keep the existing `switchbot-schedule` line; add `switchbot-ui`.) Then `pip install -e ".[dev]"`.

- [ ] **Step 2: Create the package marker and placeholder page**

`src/switchbot_scheduler/web/__init__.py`:
```python
"""Local web chat UI for the SwitchBot scheduler."""
```
`src/switchbot_scheduler/web/static/index.html` (placeholder; Task 2 replaces):
```html
<!doctype html><meta charset="utf-8"><title>SwitchBot Scheduler</title><p>ok</p>
```

- [ ] **Step 3: Write the failing tests**

`tests/test_web.py`:
```python
import json
from fastapi.testclient import TestClient
from switchbot_scheduler.web import app as webapp

CANNED = json.dumps({"schedules": [{"device": "living_room",
    "events": [{"time": "06:00", "action": "on", "days": ["mon"]}]}]})


def _client(tmp_path, monkeypatch, completion=None):
    reg = tmp_path / "devices.yaml"
    reg.write_text('devices:\n  living_room:\n    aliases: ["salon"]\n    ble_id: "U1"\n')
    monkeypatch.setenv("SWITCHBOT_DEVICES", str(reg))
    monkeypatch.setattr(webapp, "_completion_fn", completion or (lambda s, u: CANNED))
    return TestClient(webapp.app)


def test_preview_returns_readback_and_schedule(tmp_path, monkeypatch):
    body = _client(tmp_path, monkeypatch).post("/preview", json={"prompt": "salon on 6am"}).json()
    assert body["ok"] is True
    assert "living_room: on 06:00" in body["readback"]
    assert body["schedule"]["schedules"][0]["device"] == "living_room"


def test_preview_unknown_device_returns_error(tmp_path, monkeypatch):
    bad = lambda s, u: json.dumps({"schedules": [{"device": "bedroom",
        "events": [{"time": "06:00", "action": "on", "days": ["mon"]}]}]})
    body = _client(tmp_path, monkeypatch, bad).post("/preview", json={"prompt": "bedroom on"}).json()
    assert body["ok"] is False and "Unknown device" in body["error"]


def test_apply_writes_posted_schedule(tmp_path, monkeypatch):
    client = _client(tmp_path, monkeypatch)
    calls = []
    monkeypatch.setattr(webapp, "write_schedule", lambda s, r: calls.append(s))
    sched = {"schedules": [{"device": "living_room",
        "events": [{"time": "06:00", "action": "on", "days": ["mon"]}]}]}
    body = client.post("/apply", json={"schedule": sched}).json()
    assert body["ok"] is True and body["written"] == ["living_room"]
    assert len(calls) == 1 and calls[0].schedules[0].events[0].time == "06:00"


def test_apply_ble_failure_returns_error(tmp_path, monkeypatch):
    client = _client(tmp_path, monkeypatch)
    def boom(s, r):
        raise RuntimeError("couldn't reach living_room")
    monkeypatch.setattr(webapp, "write_schedule", boom)
    sched = {"schedules": [{"device": "living_room",
        "events": [{"time": "06:00", "action": "on", "days": ["mon"]}]}]}
    body = client.post("/apply", json={"schedule": sched}).json()
    assert body["ok"] is False and "reach" in body["error"]
```

- [ ] **Step 4: Run tests to verify they fail**

Run: `PYTHONPATH=src pytest tests/test_web.py -v`
Expected: FAIL — `ModuleNotFoundError: switchbot_scheduler.web.app`.

- [ ] **Step 5: Write the app**

`src/switchbot_scheduler/web/app.py`:
```python
import os
from pathlib import Path

from fastapi import FastAPI
from fastapi.responses import FileResponse
from pydantic import BaseModel

from ..registry import Registry
from ..core import build_schedule
from ..readback import readback
from ..ble_writer import write_schedule
from ..validator import validate
from ..model import Schedule, DeviceSchedule, Event

STATIC = Path(__file__).parent / "static"

# Test seam: None => parser uses the real OpenAI call; tests set a canned fn.
_completion_fn = None

app = FastAPI()


def _registry() -> Registry:
    return Registry.load(os.environ.get("SWITCHBOT_DEVICES", "devices.yaml"))


def schedule_to_json(s: Schedule) -> dict:
    return {"schedules": [
        {"device": ds.device,
         "events": [{"time": e.time, "action": e.action, "days": e.days} for e in ds.events]}
        for ds in s.schedules
    ]}


def schedule_from_json(data: dict) -> Schedule:
    return Schedule(schedules=[
        DeviceSchedule(device=s["device"], events=[
            Event(time=e["time"], action=e["action"], days=e["days"]) for e in s["events"]
        ]) for s in data["schedules"]
    ])


class PreviewReq(BaseModel):
    prompt: str


class ApplyReq(BaseModel):
    schedule: dict


@app.get("/")
def index():
    return FileResponse(STATIC / "index.html")


@app.post("/preview")
def preview(req: PreviewReq):
    try:
        registry = _registry()
        schedule = build_schedule(req.prompt, registry, completion_fn=_completion_fn)
        return {"ok": True, "readback": readback(schedule), "schedule": schedule_to_json(schedule)}
    except Exception as err:
        return {"ok": False, "error": str(err)}


@app.post("/apply")
def apply(req: ApplyReq):
    try:
        registry = _registry()
        schedule = schedule_from_json(req.schedule)
        validate(schedule, registry)
        write_schedule(schedule, registry)
        seen, written = set(), []
        for ds in schedule.schedules:
            if ds.device not in seen:
                seen.add(ds.device)
                written.append(ds.device)
        return {"ok": True, "written": written}
    except Exception as err:
        return {"ok": False, "error": str(err)}


def main():
    import uvicorn
    uvicorn.run(app, host="0.0.0.0", port=int(os.environ.get("PORT", "8000")))
```

- [ ] **Step 6: Run tests to verify they pass**

Run: `PYTHONPATH=src pytest tests/test_web.py -v`
Expected: 4 passed. Then full suite `PYTHONPATH=src pytest -q` — all pass.

- [ ] **Step 7: Commit**

```bash
git add src/switchbot_scheduler/web tests/test_web.py pyproject.toml
git commit -m "feat: FastAPI web backend (/preview, /apply) wrapping the core"
```

---

## Task 2: Frontend — the clean chat page

Visual/manual task: create the real `index.html`, then verify by eye in a browser. No unit test (UI). Replaces the Task 1 placeholder.

**Files:**
- Modify: `src/switchbot_scheduler/web/static/index.html` (replace placeholder with the full page below)

**Interfaces:**
- Consumes: `POST /preview {prompt}` → `{ok, readback, schedule}` | `{ok:false, error}`; `POST /apply {schedule}` → `{ok, written:[...]}` | `{ok:false, error}`.
- Produces: nothing (leaf).

- [ ] **Step 1: Replace `static/index.html` with the full chat UI**

`src/switchbot_scheduler/web/static/index.html`:
```html
<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>SwitchBot Scheduler</title>
<style>
  :root{
    --bg:#f7f8fa; --panel:#fff; --ink:#1c1e21; --muted:#6b7280;
    --line:#e6e8eb; --accent:#2f6df6; --accent-ink:#fff; --user:#2f6df6; --user-ink:#fff;
    --ok:#178a4c; --err:#c0362c;
  }
  @media (prefers-color-scheme: dark){
    :root{ --bg:#0f1115; --panel:#181b21; --ink:#e8eaed; --muted:#9aa0a6;
      --line:#262a31; --accent:#5b8cff; --accent-ink:#0f1115; --user:#3b6ff2; --user-ink:#fff; }
  }
  *{box-sizing:border-box}
  body{margin:0;font:16px/1.5 -apple-system,BlinkMacSystemFont,"Segoe UI",Roboto,sans-serif;
    background:var(--bg);color:var(--ink);height:100dvh;display:flex;flex-direction:column}
  header{padding:16px 20px;border-bottom:1px solid var(--line);font-weight:600}
  header small{display:block;font-weight:400;color:var(--muted);font-size:13px;margin-top:2px}
  #log{flex:1;overflow-y:auto;padding:20px;display:flex;flex-direction:column;gap:14px;
    max-width:760px;width:100%;margin:0 auto}
  .row{display:flex}
  .row.user{justify-content:flex-end}
  .bubble{max-width:80%;padding:12px 15px;border-radius:16px;white-space:pre-wrap;word-wrap:break-word}
  .user .bubble{background:var(--user);color:var(--user-ink);border-bottom-right-radius:5px}
  .bot .bubble{background:var(--panel);border:1px solid var(--line);border-bottom-left-radius:5px}
  .card{background:var(--panel);border:1px solid var(--line);border-radius:14px;padding:14px 16px;max-width:80%}
  .card h4{margin:0 0 8px;font-size:13px;color:var(--muted);font-weight:600;text-transform:uppercase;letter-spacing:.04em}
  .card .line{padding:3px 0;font-variant-numeric:tabular-nums}
  .actions{margin-top:12px;display:flex;gap:8px}
  button{font:inherit;border:0;border-radius:10px;padding:9px 16px;cursor:pointer}
  .approve{background:var(--accent);color:var(--accent-ink);font-weight:600}
  .approve:disabled{opacity:.5;cursor:default}
  .status.ok{color:var(--ok);font-weight:600}
  .status.err{color:var(--err);font-weight:600}
  form{display:flex;gap:10px;padding:14px 20px;border-top:1px solid var(--line);
    max-width:760px;width:100%;margin:0 auto}
  input{flex:1;font:inherit;padding:12px 14px;border:1px solid var(--line);border-radius:12px;
    background:var(--panel);color:var(--ink)}
  input:focus{outline:2px solid var(--accent);outline-offset:0}
</style>
</head>
<body>
<header>SwitchBot Scheduler<small>Type a schedule in Hebrew or English — you'll approve before anything is written.</small></header>
<div id="log"></div>
<form id="f"><input id="p" autocomplete="off" placeholder="e.g. turn סלון on 6am–5pm every day" autofocus><button class="approve" type="submit">Send</button></form>
<script>
const log = document.getElementById('log'), form = document.getElementById('f'), input = document.getElementById('p');

function scroll(){ log.scrollTop = log.scrollHeight; }
function add(el){ log.appendChild(el); scroll(); return el; }

function userBubble(text){
  const row = document.createElement('div'); row.className='row user';
  const b = document.createElement('div'); b.className='bubble'; b.textContent=text;
  row.appendChild(b); add(row);
}
function botBubble(text, cls){
  const row = document.createElement('div'); row.className='row bot';
  const b = document.createElement('div'); b.className='bubble'; if(cls) b.classList.add('status',cls);
  b.textContent=text; row.appendChild(b); return add(row);
}

function readbackCard(readback, schedule){
  const row = document.createElement('div'); row.className='row bot';
  const card = document.createElement('div'); card.className='card';
  const h = document.createElement('h4'); h.textContent='Here’s what I understood'; card.appendChild(h);
  readback.split('\n').forEach(line=>{
    const d = document.createElement('div'); d.className='line'; d.textContent=line; card.appendChild(d);
  });
  const actions = document.createElement('div'); actions.className='actions';
  const approve = document.createElement('button'); approve.className='approve'; approve.textContent='Approve & write';
  const hint = document.createElement('span'); hint.style.color='var(--muted)'; hint.style.alignSelf='center';
  hint.textContent='…or just type a correction';
  actions.appendChild(approve); actions.appendChild(hint); card.appendChild(actions);
  row.appendChild(card); add(row);

  approve.onclick = async ()=>{
    approve.disabled = true; approve.textContent='Writing…';
    try{
      const r = await fetch('/apply',{method:'POST',headers:{'Content-Type':'application/json'},
        body:JSON.stringify({schedule})});
      const data = await r.json();
      if(data.ok) botBubble('✅ Written to '+data.written.join(', '), 'ok');
      else { botBubble('⚠️ '+data.error, 'err'); approve.disabled=false; approve.textContent='Approve & write'; }
    }catch(e){ botBubble('⚠️ '+e, 'err'); approve.disabled=false; approve.textContent='Approve & write'; }
  };
}

form.onsubmit = async (e)=>{
  e.preventDefault();
  const prompt = input.value.trim(); if(!prompt) return;
  input.value=''; userBubble(prompt);
  const thinking = botBubble('…');
  try{
    const r = await fetch('/preview',{method:'POST',headers:{'Content-Type':'application/json'},
      body:JSON.stringify({prompt})});
    const data = await r.json(); thinking.remove();
    if(data.ok) readbackCard(data.readback, data.schedule);
    else botBubble('⚠️ '+data.error, 'err');
  }catch(e){ thinking.remove(); botBubble('⚠️ '+e, 'err'); }
};
</script>
</body>
</html>
```

- [ ] **Step 2: Launch and verify by eye**

Run:
```bash
OPENAI_API_KEY=... PYTHONPATH=src python -m switchbot_scheduler.web.app
```
(or `switchbot-ui` after install). Open `http://localhost:8000`. Confirm: clean layout, light/dark follows the OS, typing a prompt shows a read-back card with an Approve button, an obviously-bad prompt (e.g. "bedroom on") shows a friendly error bubble. (Approving does a real BLE write — only when home near the Bots.)

- [ ] **Step 3: Commit**

```bash
git add src/switchbot_scheduler/web/static/index.html
git commit -m "feat: clean chat UI page (preview card + approve, light/dark)"
```

---

## Self-Review

**Spec coverage:** core loop (prompt→readback→approve/refine→write) = Task 1 endpoints + Task 2 UI ✅; `/preview` no-write & `/apply` writes-exact-approved ✅ (Task 1 Step 5); errors as friendly messages ✅ (endpoints return `{ok:false,error}`, UI renders err bubble); FastAPI + uvicorn + httpx, `switchbot-ui`, env config, 0.0.0.0 bind ✅ (Task 1 Steps 1,5); clean/minimal + light/dark ✅ (Task 2); tests network/hardware-free via `_completion_fn` seam + monkeypatched `write_schedule` ✅ (Task 1 Step 3); manual e2e ✅ (Task 2 Step 2). Out-of-scope items correctly absent.

**Placeholder scan:** No TBDs; all code complete. The Task 1 `static/index.html` placeholder is intentional and replaced in Task 2.

**Type consistency:** `schedule_to_json`/`schedule_from_json` mirror the `{schedules:[{device,events:[{time,action,days}]}]}` shape used in `/preview` output and `/apply` input and the JS `schedule` payload. `write_schedule(schedule, registry)` and `build_schedule(prompt, registry, completion_fn)` match the core signatures. `_completion_fn` seam matches the CLI/parser pattern.
