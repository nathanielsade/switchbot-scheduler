__pycache__/
*.pyc
.venv/
.env
*.egg-info/
.pytest_cache/
secrets.yaml
# Roborock cloud auth token (contains account tokens — never commit)
roborock_userdata.json
# Home-agent runtime data (conversation history)
*.db
home_agent.db
secrets/
# Finance collector deps (installed via npm ci from collector/package-lock.json)
collector/node_modules/
